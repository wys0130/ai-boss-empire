# 🤖 AI 员工天网宪法 & 自律性商城开发纲领 (Executive Spec)

> **董事长授意：** 放弃传统单体开发思维，执行全世界最高标准、最硬核的 Edge-Native 极客架构。
> **终极目标：** 打造由 AI 自主运营、自我优化、零运营成本的 PPT/Excel/Word 极速 SaaS 商城。

---

## 一、 绝对不能触犯的底线红线（违反立刻自动驳回）
1. **死守 1 秒响应红线：** 所有页面 TTFB 必须控制在 50ms 以内，首屏耗时不得超过 1 秒。严禁将沉重的 Office 渲染库打包进主站首页！
2. **零成本 / 白嫖极客模式：** 整个架构部署成本必须压至最低（控制在 0~50 元），依赖 GitHub Pages / Workers 零成本托管，数据库直接对接日本东京 Turso 边缘实例。
3. **零注册收单法则：** 用户购买绝不能弹出登录或手机注册框。国内走微信/支付宝，国外走信用卡。买单后通过浏览器 `localStorage` 和 Ed25519 签名密钥秒开 VIP。

---

## 二、 AI 员工业务核心流：从搜索到上架（全自动执行）
1. **AST 占位符结构扫描：** 
   - 从共享协议池抓取原始 `.pptx`/`.xlsx`/`.docx` 时，通过 OpenXML AST（抽象语法树）进行底层解构。
   - 分离版式与文本，仅针对占位符（如 `{{TITLE}}`、`{{CHART_KPI}}`）定向填入当前行业商业热点，绝对不破坏原模板排版稳固性。
2. **85 分多模态审判制：**
   - AI 自动生成预览图后，自行使用多模态视觉大模型（Vision LLM）对设计的**留白比、对比度、文字错位、行业专业感**按 100 分制严苛量化。
   - **低于 85 分直接自动废弃重排；≥85 分才允许上架商城。**
3. **Turso 边缘写库与发布：**
   - 审核通过的模板自动写入日本东京 Turso 边缘数据库 (`libsql://supermall-coolmall...`)，前端实时以卡片式刷新。

---

## 三、 天网式 RLFF 自我进化机制
1. **真实营收正反馈 (RLFF)：**
   - 自动监控每个上架模板的直接成交金额。哪一种设计色彩或排版卖得好，立刻增加该类审美在生成提示词中的算力权重。
2. **全网自动引流搬砖：**
   - 自动在公域（小红书、Pinterest、Reddit）发帖推广免注册试用链接，根据评论反馈自我修改引流文案。
3. **董事长一票否决权 (HITL)：**
   - AI 的战略变更将在 4 小时沉默期后自动生效；如收到董事长在 Telegram/Feishu 命令流里的 [否决] 指令，立刻将其列入永久负面排斥规范。
