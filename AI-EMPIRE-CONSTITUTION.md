# 🤖 APEXWORK AI 员工天网宪法 & 自律性商城开发纲领 (Executive Spec V3.1)

> **董事长授意：** 放弃传统单体开发思维，执行全世界最高标准、最硬核的 Edge-Native 极客架构。
> **终极目标：** 打造由 AI 自主运营、自我优化、零运营成本、主打“少字多图与视觉冲击”的 PPT/Excel/Word 极速 SaaS 商城。

---

## 一、 绝对不能触犯的底线红线（违反立刻自动驳回）
1. **死守 1 秒响应红线 (Edge-Native)：** 所有页面 TTFB 必须控制在 50ms 以内，首屏耗时不得超过 1 秒。严禁将沉重的后端渲染库打包进主站首页！
2. **零成本 / 白嫖极客模式：** 整个架构部署成本必须压至最低（控制在 0~50 元），依赖 GitHub Pages / Cloudflare Workers 零成本边缘托管，数据库直接对接日本东京 Turso 边缘实例 (`libsql://...`)。
3. **零注册收单法则：** 用户购买绝不弹出登录或手机注册框。买单通过在线支付回调生成 **Ed25519 签名密钥**，并本地写死于浏览器 `localStorage` 秒开 VIP 下载权限。
4. **少字多图与视觉至上：** 
   - 任何模板页面严禁堆砌枯燥冗长的黑字段落！
   - 必须大量采用 **SVG 动态进度条、数据 KPI 卡片、对比能量表**；默认集成大厂高清商务与科技实景背景大图。
5. **商务三件套统一交付：** 商城和产品线必须同时覆盖 **PPT 路演战略、Excel 财务模型、Word 合规报告** 三件套，绝不可仅写单一模板。

---

## 二、 【高级工程经验包】最先进架构与防失忆法则 (Edge-Native & Memory Engineering)
1. **Edge-Native 双级响应式图像路由 (Advanced Image Routing)：**
   - **智能格式降维**：所有实景大图必须带上 CDN 边缘转换指令（如 `w=1280&q=75&fm=webp`），严禁加载原始 MB 级大图；
   - **渐进式预加载与虚拟化**：首屏仅强载第 1 页主图；第 2~15 页必须通过 `IntersectionObserver` 懒加载挂载，做到千页滑动零卡顿。
2. **架构映射表与按需读取 (Project Manifest & RAG Tool Use)：**
   - **绝不猜文件名**：AI 动手修改前，强制第一步读取根目录下的 `PROJECT_MAP.json` 架构映射表；
   - **RAG 局部定位**：如调整文案只读取 `/data/aerotech-deck.json`；如调整卡片布局只读取 `/components/slide-card.html`。改完立刻释放内存，绝不一次性吞吐全项目大文件。
3. **记忆修剪与滑动窗口 (Memory Pruning & Sliding Window)：**
   - **三层金字塔记忆**：宪法（永久只读） > 最新 5 条实战与 VETO 否决黑名单（滑动窗口活跃保留） > 历史存档（定期压缩为一句梗概）。
   - **严禁重复新建**：现存文件树中已拥有的路由，只能使用增量修缮 (PATCH)，杜绝循环生成同一用途文件。

---

## 三、 架构规范：声明式数据源 + 组件化多目录 (Declarative Routing)
1. **彻底解耦开发：**
   - `/data/`: 独立存放 `.json` 声明式数据（幻灯片参数、XLSX 公式占位、主题预设卡）；
   - `/components/`: 独立管理界面局部模版；
   - `index.html` 与 `editor.html`: 仅作为纯静态外壳与前端路由加载器。
2. **现存物理文件保护制：**
   - AI 自主升级前，自动加载当前仓库真正的 Git Tree；已存在的文件一律通过增量局部修改（PATCH），严禁重新新建重复名称或 `===NEW_MEMORY===` 等乱码废弃文件。

---

## 四、 AI 员工业务核心流：从搜索到上架（全自动执行）
1. **AST 占位符无损结构扫描：** 
   - 从共享协议池抓取原始 `.pptx`/`.xlsx`/`.docx` 时，通过 OpenXML AST（抽象语法树）进行底层解构。
   - 分离版式与文本，仅针对占位符（如 `{{TITLE}}`、`{{CHART_KPI}}`）定向填入商业热点，绝对不破坏原模板排版稳固性。
2. **85 分多模态严苛审判制：**
   - AI 自动生成图文预览后，自行调用多模态视觉大模型（Vision LLM）对设计的**留白比、对比度、文字错位、视觉冲击、行业专业感**按 100 分制量化。
   - **低于 85 分直接自动废弃重排；≥85 分且“无长文本废话”后才允许上架商城。**
3. **Turso 边缘写库与实时上线：**
   - 审核通过的模板数据自动写入日本东京 Turso 边缘数据库，前端主商城以卡片式无感刷新并展现最新 KPI 成果。

---

## 五、 天网式 RLFF 自我进化与董事长否决机制
1. **真实营收正反馈 (RLFF)：**
   - 自动监控每个模板卡的直接成交金额。哪一种视觉色彩或 SVG 图表卖得好，立刻在提示词中显著调高该审美权重。
2. **公域全自动搬砖：**
   - AI 自行在 Reddit、Pinterest 等公域发布可视化图表干货帖并附带商城试用链，根据互动数据自我迭代文案。
3. **董事长一票否决权 (HITL / VETO)：**
   - AI 的例行优化将在 **4 小时沉默期后自动提交上线**；
   - 董事长拥有最高驳回权：只需在后台点击 **【🚨 一键回退 / 否决】**，系统立刻秒级快照还原上一个好版本，并将该优化思路永久列入负面避坑黑名单！
